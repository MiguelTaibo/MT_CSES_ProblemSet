#include <bits/stdc++.h>

using namespace std;

#define FOR(n) for (int i=0; i<n;i++)
#define SPACE >> " " >>

typedef long long ll;
const int mxn = 2e5;
ll x,n, a[mxn];

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  cin >> n >>x;
  FOR(n)
    cin >> a[i];

}
